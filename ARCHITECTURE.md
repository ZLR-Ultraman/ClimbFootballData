# ClimbFootballData 架构说明

本文档用于约束后续所有新增功能、修改和重构必须遵循的项目架构与边界。**后续开发必须先对齐本文档，再开始改代码。**

## 1. 项目目标

本项目是一个基于 Titan007 数据源的足球数据爬取与查询平台，主要能力包括：

- 按日期或比赛 ID 查询比赛数据
- 批量爬取比赛列表与详情
- 实时查看当前爬取进度与日志
- 查看历史批次日志与查询记录
- 将爬取结果写入本地数据库并支持回查

---

## 2. 核心架构原则

### 2.1 职责分离

必须严格遵循“路由层、服务层、数据层、展示层分离”原则。

- **路由层**：只负责接收请求、参数校验、返回 JSON 或页面
- **服务层**：封装业务逻辑、状态管理、日志读取、代理、爬虫执行
- **数据层**：只负责数据库读写，不承载业务判断
- **展示层**：只负责页面结构与交互，不直接写业务逻辑

### 2.2 不破坏现有功能

所有修改必须保证以下能力不回退：

- 比赛查询可用
- 批量爬取可用
- 实时爬取日志可用
- 历史日志查看可用
- 代理访问可用
- 数据写入与回查可用

### 2.3 渐进式重构

重构必须采用“先抽离、再替换、最后清理”的方式，不能一次性大改导致功能失效。

---

## 3. 当前项目实际目录结构

当前项目已经按职责拆分，推荐并实际采用如下结构：

```text
ClimbFootballData/
├─ app.py
├─ db_manager.py
├─ ARCHITECTURE.md
├─ README.md
├─ test_run.py
├─ football_data.sqlite3
│
├─ services/
│  ├─ crawler_service.py
│  ├─ crawl_state_service.py
│  ├─ log_service.py
│  └─ proxy_service.py
│
├─ scraper/
│  └─ titan_scraper.py
│
├─ templates/
│  └─ index.html
│
└─ static/
   └─ js/
      └─ app.js
```

### 目录说明

- `app.py`：Flask 路由入口
- `db_manager.py`：数据库写入与查询基础能力
- `services/`：业务服务层
- `scraper/`：Titan007 爬虫实现
- `templates/`：页面结构模板
- `static/js/`：前端交互脚本

---

## 4. 模块职责说明

## 4.1 `app.py` —— Flask 路由入口

`app.py` 是整个系统的路由编排中心，职责仅限于：

- 注册 Flask 路由
- 接收前端请求
- 调用对应服务层
- 返回页面或 JSON 响应

### `app.py` 不应承担的职责

- 不应直接编写复杂 SQL
- 不应直接承担爬虫主流程
- 不应直接承担代理重写逻辑
- 不应直接承担日志查询聚合逻辑
- 不应直接承担前端状态管理

---

## 4.2 `db_manager.py` —— 数据写入与业务表读写

该模块负责数据库的基础访问能力，包括：

- 比赛数据写入
- 爬取批次写入
- 爬取会话写入
- 爬虫状态持久化
- 日志写入
- 比赛数据查询

### 要求

- 保持数据库访问语义稳定
- 不在路由层直接拼复杂 SQL
- 新增表或字段时要保证兼容旧功能

---

## 4.3 `services/crawler_service.py` —— 爬虫执行服务

该模块封装批量爬取执行流程，负责：

- 批次启动
- 日期循环抓取
- 单场比赛详情抓取
- 结果入库
- 批次/会话状态更新
- 停止信号处理
- 爬取异常处理

### 约束

- 只负责“怎么爬”，不负责“页面怎么展示”
- 不直接操作前端控件
- 不输出 UI 逻辑
- 保留原有日志写入行为

---

## 4.4 `services/crawl_state_service.py` —— 状态与批次汇总服务

该模块负责通用状态工具函数，包括：

- 重置爬虫状态
- 更新批次汇总
- 生成日期区间

### 约束

- 仅放通用状态工具
- 不放与路由强绑定的逻辑
- 不放爬虫执行主循环

---

## 4.5 `services/log_service.py` —— 日志查询服务

该模块负责从数据库读取日志及批次信息，包括：

- 查询所有历史日志
- 按批次筛选
- 按类型筛选
- 按时间范围筛选
- 获取批次下拉选项

### 约束

- 只读，不写
- 不修改实时爬取日志逻辑
- 不在前端直接访问数据库

---

## 4.6 `services/proxy_service.py` —— 代理服务

该模块负责 Titan007 页面及静态资源代理，包括：

- 代理请求转发
- HTML 链接重写
- 静态资源路径重写

### 约束

- 只处理代理与重写
- 不混入查询或爬虫逻辑
- 不修改源站业务语义

---

## 4.7 `scraper/titan_scraper.py` —— 爬虫实现

该模块负责 Titan007 数据抓取的具体实现，包括：

- 列表页抓取
- 详情页抓取
- 比赛数据解析
- 近期战绩格式化
- 单场比赛同步写入准备

### 约束

- 只负责爬取细节实现
- 不承载 Flask 路由逻辑
- 不承载页面状态逻辑

---

## 4.8 `templates/index.html` —— 页面结构层

该文件只允许保留：

- 页面骨架
- Vue 容器
- 样式定义（如暂未拆分）
- 脚本引用

### 约束

- 不再继续堆积大段业务 JS
- 不直接写复杂业务流程
- 页面结构和功能入口要清晰

---

## 4.9 `static/js/app.js` —— 前端交互逻辑

该文件承载前端交互逻辑，包括：

- 比赛查询
- 批量爬取控制
- 爬取状态轮询
- 实时日志查看
- 历史日志查看入口
- 弹窗、抽屉、筛选逻辑

### 约束

- 只负责前端交互，不直接接触数据库
- 所有接口请求都走后端 API
- 逻辑要尽量模块化，避免继续膨胀成“第二个模板文件”

---

## 5. 数据流规范

### 5.1 查询数据流

前端查询比赛数据时，应遵循：

1. 前端发起请求
2. `app.py` 接收请求
3. 调用数据库查询接口
4. 返回比赛列表或单场比赛结果
5. 前端仅负责展示

### 5.2 批量爬取数据流

批量爬取时，应遵循：

1. 前端发起批量爬取请求
2. `app.py` 创建或启动爬虫任务
3. `services/crawler_service.py` 执行抓取与入库
4. `db_manager.py` 写入批次、会话、日志、比赛数据
5. 前端通过状态接口轮询更新

### 5.3 日志数据流

日志必须分成两类展示逻辑：

- **实时日志**：用于当前正在爬取时展示，读 `crawl_state`
- **历史日志**：用于非爬取状态下查看全部记录，读数据库 `crawl_logs`

> 注意：历史日志入口必须能查看数据库中的全部日志，而不是只看当前内存状态。

---

## 6. 批次与日志设计规范

### 6.1 批次定义

批次是一个“有明确开始与结束边界”的任务单元。当前项目中，批次主要有两种来源：

- 批量爬取任务批次
- 用户一次查询/操作对应的记录批次（如后续需要扩展）

### 6.2 批次字段要求

批次记录尽量包含：

- `batch_id`
- `start_date`
- `end_date`
- `status`
- `total_days`
- `finished_days`
- `success_days`
- `failed_days`
- `total_matches`
- `qualified_matches`
- `skipped_matches`
- `failed_matches`
- `started_at`
- `finished_at`
- `error_message`

### 6.3 日志字段要求

日志记录尽量包含：

- `batch_id`
- `crawl_date`
- `match_id`
- `level`
- `log_type`
- `message`
- `created_at`

---

## 7. 前端交互规范

### 7.1 页面功能入口

页面上任何新增功能入口都必须遵循：

- 入口位置明确
- 功能职责单一
- 不影响现有查询与爬取入口

### 7.2 日志入口规范

日志入口分两种：

- **实时日志入口**：服务于当前爬取任务
- **历史日志入口**：服务于数据库全部日志查看

两者不能混用，也不能相互替代。

### 7.3 组件状态规范

前端状态应尽量分为：

- 查询状态
- 爬取状态
- 日志状态
- 弹窗状态

避免把所有状态塞进一个巨大对象里而无法维护。

---

## 8. 新增功能必须遵守的执行流程

后续每次新增功能或修改，都必须按以下顺序执行：

### 第一步：先分析归属

先判断新需求属于：

- 路由层
- 服务层
- 数据层
- 展示层

### 第二步：先找已有模块

如果已有模块能承接，优先扩展已有模块，不要随意新建重复模块。

### 第三步：先保证兼容

任何改动都必须保证：

- 旧接口不失效
- 旧页面不崩
- 旧数据不丢
- 旧状态不乱

### 第四步：再写中文注释

新增函数、类、重要分支、复杂逻辑必须写中文注释，说明：

- 做什么
- 为什么这么做
- 与哪些模块协作

### 第五步：最后做验证

修改后必须检查：

- 关键文件语法
- 接口返回是否兼容
- 页面入口是否正常
- 日志与批次展示是否正常

---

## 9. 导入路径规范

由于项目已经按目录拆分，后续所有代码都要遵守新的导入路径。

### 9.1 服务层导入

应使用：

- `from services.log_service import LogService`
- `from services.crawl_state_service import reset_crawl_state, update_batch_summary`
- `from services.crawler_service import CrawlerService`
- `from services.proxy_service import build_proxy_target_url, proxy_request`

### 9.2 爬虫层导入

应使用：

- `from scraper.titan_scraper import TitanScraper`

### 9.3 禁止事项

禁止继续使用旧路径：

- `from log_service import ...`
- `from crawl_state_service import ...`
- `from crawler_service import ...`
- `from proxy_service import ...`
- `from titan_scraper import ...`

---

## 10. 代码修改红线

以下行为禁止：

- 在 `app.py` 里继续堆积大型业务实现
- 在前端模板里继续堆积大段 JS 业务逻辑
- 直接改数据库结构而不考虑兼容
- 不经说明就删除已有接口
- 把实时日志和历史日志混为一谈
- 修改爬虫流程时不保留日志与批次写入

---

## 11. 建议的目录演进方向

后续若继续扩展，建议逐步演进为：

```text
ClimbFootballData/
├─ app.py
├─ db_manager.py
├─ ARCHITECTURE.md
├─ README.md
├─ test_run.py
├─ football_data.sqlite3
├─ services/
├─ scraper/
├─ templates/
└─ static/
```

如果后续继续拆分，可以再考虑：

- `routes/`
- `repositories/`
- `models/`

但是否拆分到这一层，需根据实际复杂度决定，不建议过度拆分。

---

## 12. 最终约束

**所有后续新增功能和修改，都必须遵守本文档的架构分层与职责边界。**

如果某次修改必须临时违反本文档，必须先说明原因、影响范围和回滚方式，再执行。
